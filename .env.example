# Configurações do Django
DJANGO_SECRET_KEY=your-secret-key-here-generate-a-new-one
DJANGO_DEBUG=True
DJANGO_ENVIRONMENT=development
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# Configurações de Banco de Dados (Para Produção - PostgreSQL)
DB_NAME=sade_prod
DB_USER=sade_user
DB_PASSWORD=your-database-password
DB_HOST=localhost
DB_PORT=5432

# Configurações de Email (Para Produção)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password

# Configurações de Cache (Para Produção)
CACHE_LOCATION=127.0.0.1:11211

# Configurações de Segurança (Para Produção)
SECURE_SSL_REDIRECT=True
SESSION_COOKIE_SECURE=True
CSRF_COOKIE_SECURE=Trues do Django
DJANGO_SECRET_KEY=sua-chave-secreta-aqui
DJANGO_DEBUG=False
DJANGO_ALLOWED_HOSTS=seu-dominio.com,www.seu-dominio.com

# Configurações do Banco de Dados (para produção)
# DATABASE_URL=postgres://usuario:senha@localhost:5432/nome_do_banco

# Configurações de Email (opcional)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=seu-email@gmail.com
# EMAIL_HOST_PASSWORD=sua-senha-de-aplicativo
